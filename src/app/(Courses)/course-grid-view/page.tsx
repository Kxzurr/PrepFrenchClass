import CourseGridView from "@/src/components/Courses/GridView";

export const metadata = {
  title: "Course Grid View | Prep French Classes",
  description: "Browse all our French courses in grid view format",
};

export default function CourseGridViewPage() {
  return <CourseGridView />;
}
